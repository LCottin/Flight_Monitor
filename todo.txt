Dans un premier temps, lire uniquement une donnée pour chaque avion

puis

Une seule base de données courante issue du web, database enregistre les time stamp et chaque plane ses positions

https://openskynetwork.github.io/opensky-api/rest.html
curl -s "https://opensky-network.org/api/states/all" | python3 -m json.tool 